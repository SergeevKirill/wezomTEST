<?php

return [
    'menu' => config('mock.menu', []),
    'item_menu' => config('mock.item_menu', []),
    'form_upper_tabs' => config('mock.form_upper_tabs', []),
    'inputs' => config('mock.inputs', []),
    'items' => config('mock.items', []),
    'footer' => config('mock.footer', []),
    'item_spec' => config('mock.item_spec', []),
    'item_advantages' => config('mock.item_advantages', []),
    'characteristics_table', config('mock.characteristics_table', []),
    'item_tabs', config('mock.item_tabs', []),
    'offer_tab', config('mock.offer_tab', []),
    'text_tab', config('mock.text_tab', []),
    'comments', config('mock.comments', []),
    'guarantees', config('mock.guarantees', []),
    'item_description', config('mock.item_description', []),
    'item_price', config('mock.item_price', []),
    'creator', config('mock.creator', []),
    'diameter', config('mock.diameter', []),
    'catalog_items', config('mock.catalog_items', []),
    'cart_items', config('mock.cart_items', [])
];

